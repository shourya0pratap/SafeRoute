{
  "name": "saferoute-app",
  "private": true,
  "version": "1.0.0",
  "description": "SafeRoute — Navigate safer, not just faster",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
